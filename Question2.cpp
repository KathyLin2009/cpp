#include <iostream> 
#include <vector>
#include <algorithm>
using namespace std;

bool CheckArray(vector<int> a, vector<int> b)
{
    for(int i = 0, i < a.size(), i++)
    {
        for(int j = 0, j < b.size(), j++)
        {
            if(b[j] == a[i+j])
            {
                continue;
            }
            else if(j == b.size() - 1)

            `
            else
            {
                
            }
        }
        
    }


}

int main()
{

}